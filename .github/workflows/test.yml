name: Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - name: Set up tree-sitter
        uses: tree-sitter/setup-action/cli@v2
      - name: Run corpus tests
        run: tree-sitter test
      - name: Parse example .bat/.cmd files (integration test)
        # Stats-only: some fixtures (Library.bat, paths.bat, zap.bat) are known to
        # have a small number of ERROR nodes. The next step enforces zero errors on
        # the fixtures we expect to parse cleanly.
        run: tree-sitter parse examples/*.bat examples/*.cmd --quiet --stat || true
      - name: Assert zero parse errors on real-world fixtures
        run: |
          for fixture in \
            examples/search.bat \
            examples/dope.bat \
            examples/2note2.bat \
            examples/AddPaths.bat \
            examples/AutoRun.cmd \
            examples/CheckEOL.bat \
            examples/TimeX.bat \
            examples/mvn.cmd \
          ; do
            output=$(tree-sitter parse "$fixture")
            if echo "$output" | grep -qE '\(ERROR|MISSING'; then
              echo "$fixture has parse errors:"
              echo "$output" | grep -E '\(ERROR|MISSING' | head -20
              exit 1
            fi
            echo "$fixture parsed cleanly with no ERROR or MISSING nodes."
          done
