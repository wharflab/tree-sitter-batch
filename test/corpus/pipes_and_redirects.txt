================
Simple pipe
================

dir | find "test"

---

(program
  (pipe_stmt
    (cmd
      (command_name))
    (cmd
      (command_name)
      (argument_list
        (string)))))

================
Simple redirect
================

echo hello > output.txt

---

(program
  (redirect_stmt
    (cmd
      (command_name)
      (argument_list
        (argument_value)))
    (redirection
      (redirect_op)
      (redirect_target))))

================
Redirect to nul
================

dir > nul

---

(program
  (redirect_stmt
    (cmd
      (command_name))
    (redirection
      (redirect_op)
      (redirect_target))))

================
Stderr redirect
================

dir 2>nul

---

(program
  (redirect_stmt
    (cmd
      (command_name))
    (redirection
      (redirect_op)
      (redirect_target))))

================
Stdin redirect with file descriptor
================

sort 0<input.txt

---

(program
  (redirect_stmt
    (cmd
      (command_name))
    (redirection
      (redirect_op)
      (redirect_target))))

================
Chained pipes
================

type file.txt | sort | more

---

(program
  (pipe_stmt
    (pipe_stmt
      (cmd
        (command_name)
        (argument_list
          (argument_value)))
      (cmd
        (command_name)))
    (cmd
      (command_name))))

================
Redirect fd to fd (2>&1)
================

cmd 2>&1

---

(program
  (redirect_stmt
    (cmd
      (command_name))
    (redirection
      (fd_redirect))))

================
Redirect to file then fd
================

cmd >nul 2>&1

---

(program
  (redirect_stmt
    (cmd
      (command_name))
    (redirection
      (redirect_op)
      (redirect_target)
      (fd_redirect))))

================
Redirect with echo then fd
================

echo out > file.txt 2>&1

---

(program
  (redirect_stmt
    (cmd
      (command_name)
      (argument_list
        (argument_value)))
    (redirection
      (redirect_op)
      (redirect_target)
      (fd_redirect))))

================
CALL with redirect
================

call foo > log.txt

---

(program
  (redirect_stmt
    (call_stmt
      (command_name))
    (redirection
      (redirect_op)
      (redirect_target))))

================
CALL in pipe
================

call foo | findstr bar

---

(program
  (pipe_stmt
    (call_stmt
      (command_name))
    (cmd
      (command_name)
      (argument_list
        (argument_value)))))
