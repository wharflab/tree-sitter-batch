================
Simple pipe
================

dir | find "test"

---

(program
  (pipe_stmt
    (cmd
      (command_name))
    (cmd
      (command_name)
      (argument_list
        (string)))))

================
Simple redirect
================

echo hello > output.txt

---

(program
  (redirect_stmt
    (cmd
      (command_name)
      (argument_list
        (argument_value)))
    (redirection
      (redirect_op)
      (redirect_target))))

================
Redirect to nul
================

dir > nul

---

(program
  (redirect_stmt
    (cmd
      (command_name))
    (redirection
      (redirect_op)
      (redirect_target))))

================
Stderr redirect
================

dir 2>nul

---

(program
  (redirect_stmt
    (cmd
      (command_name))
    (redirection
      (redirect_op)
      (redirect_target))))

================
Stdin redirect with file descriptor
================

sort 0<input.txt

---

(program
  (redirect_stmt
    (cmd
      (command_name))
    (redirection
      (redirect_op)
      (redirect_target))))

================
Chained pipes
================

type file.txt | sort | more

---

(program
  (pipe_stmt
    (pipe_stmt
      (cmd
        (command_name)
        (argument_list
          (argument_value)))
      (cmd
        (command_name)))
    (cmd
      (command_name))))

================
Redirect fd to fd (2>&1)
================

cmd 2>&1

---

(program
  (redirect_stmt
    (cmd
      (command_name))
    (redirection
      (fd_redirect))))

================
Redirect to file then fd
================

cmd >nul 2>&1

---

(program
  (redirect_stmt
    (cmd
      (command_name))
    (redirection
      (redirect_op)
      (redirect_target)
      (fd_redirect))))

================
Redirect with echo then fd
================

echo out > file.txt 2>&1

---

(program
  (redirect_stmt
    (cmd
      (command_name)
      (argument_list
        (argument_value)))
    (redirection
      (redirect_op)
      (redirect_target)
      (fd_redirect))))

================
CALL with redirect
================

call foo > log.txt

---

(program
  (redirect_stmt
    (call_stmt
      (command_name))
    (redirection
      (redirect_op)
      (redirect_target))))

================
Multiple redirections
================

cmd >out.txt 2>err.txt <in.txt

---

(program
  (redirect_stmt
    (cmd
      (command_name))
    (redirection
      (redirect_op)
      (redirect_target)
      (redirect_op)
      (redirect_target)
      (redirect_op)
      (redirect_target))))

================
CALL in pipe
================

call foo | findstr bar

---

(program
  (pipe_stmt
    (call_stmt
      (command_name))
    (cmd
      (command_name)
      (argument_list
        (argument_value)))))

================
Leading redirect before command
================

>NUL copy /y NUL + NUL /a

---

(program
  (redirect_stmt
    (redirection
      (redirect_op)
      (redirect_target))
    (cmd
      (command_name)
      (argument_list
        (command_option)
        (argument_value)
        (argument_value)
        (argument_value)
        (command_option)))))

================
Redirect stdout to stderr via fd
================

echo msg >&2

---

(program
  (redirect_stmt
    (cmd
      (command_name)
      (argument_list
        (argument_value)))
    (redirection
      (fd_redirect))))

================
Leading fd-redirect then command
================

>&2 echo warning

---

(program
  (redirect_stmt
    (redirection
      (fd_redirect))
    (cmd
      (command_name)
      (argument_list
        (argument_value)))))
