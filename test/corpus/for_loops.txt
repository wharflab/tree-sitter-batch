================
FOR basic
================

for %%i in (*.txt) do echo %%i

---

(program
  (for_stmt
    (for_variable)
    (for_set
      (for_set_literal))
    (cmd
      (command_name)
      (argument_list
        (variable_reference)))))

================
FOR with variable reference in set
================

for %%a in (%gimp_loc%) do echo %%a

---

(program
  (for_stmt
    (for_variable)
    (for_set
      (variable_reference))
    (cmd
      (command_name)
      (argument_list
        (variable_reference)))))

================
FOR with multiple variable references in set
================

for %%a in (%path_a% %path_b%) do echo %%a

---

(program
  (for_stmt
    (for_variable)
    (for_set
      (variable_reference)
      (for_set_literal)
      (variable_reference))
    (cmd
      (command_name)
      (argument_list
        (variable_reference)))))

================
FOR with delayed expansion variable in set
================

for %%a in (!my_list!) do echo %%a

---

(program
  (for_stmt
    (for_variable)
    (for_set
      (variable_reference))
    (cmd
      (command_name)
      (argument_list
        (variable_reference)))))

================
FOR /L numeric
================

for /l %%i in (1,1,10) do echo %%i

---

(program
  (for_stmt
    (for_options)
    (for_variable)
    (for_set
      (for_set_literal))
    (cmd
      (command_name)
      (argument_list
        (variable_reference)))))

================
FOR /F file parsing
================

for /f "tokens=1,2 delims=," %%a in (data.csv) do echo %%a

---

(program
  (for_stmt
    (for_options)
    (for_variable)
    (for_set
      (for_set_literal))
    (cmd
      (command_name)
      (argument_list
        (variable_reference)))))

================
FOR /R recursive
================

for /r %%f in (*.log) do echo %%f

---

(program
  (for_stmt
    (for_options)
    (for_variable)
    (for_set
      (for_set_literal))
    (cmd
      (command_name)
      (argument_list
        (variable_reference)))))

================
FOR /R with env var path
================

for /r %TEMP% %%i in (*.tmp) do echo %%i

---

(program
  (for_stmt
    (for_options)
    (for_variable)
    (for_set
      (for_set_literal))
    (cmd
      (command_name)
      (argument_list
        (variable_reference)))))

================
FOR /F with nested parens in set
================

for /f "tokens=1 delims=#" %%a in ('prompt #$H# & for %%b in (1) do rem') do set "DEL=%%a"

---

(program
  (for_stmt
    (for_options)
    (for_variable)
    (for_set
      (for_set_literal)
      (variable_reference)
      (for_set_literal)
      (for_set_group
        (for_set_literal))
      (for_set_literal))
    (variable_assignment
      (set_keyword)
      (variable_name)
      (quoted_assignment_value
        (variable_reference)))))

================
Macro-style FOR loop with variable prefix
================

%FOREACHLINE% %%i in ('cmd') do (
  echo %%i
)

---

(program
  (for_stmt
    (variable_reference)
    (for_variable)
    (for_set
      (for_set_literal))
    (parenthesized
      (cmd
        (command_name)
        (argument_list
          (variable_reference))))))

================
FOR loop with multi-line set
================

for %%x in ("a"
            "b"
           ) do echo %%x

---

(program
  (for_stmt
    (for_variable)
    (for_set
      (for_set_literal)
      (for_set_literal)
      (for_set_literal))
    (cmd
      (command_name)
      (argument_list
        (variable_reference)))))
