================
SET simple variable
================

set MY_VAR=hello

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (assignment_value
      (assignment_literal))))

================
SET quoted variable
================

set "MY_VAR=hello world"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (quoted_assignment_value
      (assignment_literal))))

================
SET /a arithmetic
================

set /a result=5

---

(program
  (variable_assignment
    (set_keyword)
    (arithmetic_assignment
      (set_option)
      (arithmetic_expression))))

================
SET /a complex expression
================

set /a x=y+1

---

(program
  (variable_assignment
    (set_keyword)
    (arithmetic_assignment
      (set_option)
      (arithmetic_expression))))

================
SET /a quoted expression
================

set /a "a=b*c"

---

(program
  (variable_assignment
    (set_keyword)
    (arithmetic_assignment
      (set_option)
      (arithmetic_expression))))

================
SET /a quoted arithmetic percent is not a variable reference
================

set /a "x %=5"

---

(program
  (variable_assignment
    (set_keyword)
    (arithmetic_assignment
      (set_option)
      (arithmetic_expression))))

================
SET /a quoted without space before expression
================

set /a"x=1"

---

(program
  (variable_assignment
    (set_keyword)
    (arithmetic_assignment
      (set_option)
      (arithmetic_expression))))

================
SET /p prompt
================

set /p answer=Enter value:

---

(program
  (variable_assignment
    (set_keyword)
    (prompt_assignment
      (set_option)
      (variable_name)
      (assignment_value
        (assignment_literal)))))

================
SET /p empty prompt
================

set /p name=

---

(program
  (variable_assignment
    (set_keyword)
    (prompt_assignment
      (set_option)
      (variable_name))))

================
SET /a without space before switch
================

set/a x=1

---

(program
  (variable_assignment
    (set_keyword)
    (arithmetic_assignment
      (set_option)
      (arithmetic_expression))))

================
SET /p without space before switch
================

set/p name=Enter:

---

(program
  (variable_assignment
    (set_keyword)
    (prompt_assignment
      (set_option)
      (variable_name)
      (assignment_value
        (assignment_literal)))))

================
SET /p without any spaces around switch
================

set/pname=Enter:

---

(program
  (variable_assignment
    (set_keyword)
    (prompt_assignment
      (set_option)
      (variable_name)
      (assignment_value
        (assignment_literal)))))

================
SET empty value
================

set MY_VAR=

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)))

================
SET quoted empty value
================

set "MY_VAR="

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)))

================
SET variable with multiple quoted backslash paths
================

SET python_loc="\bin\python" "\32\bin\python" "\Python\python"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (assignment_value
      (assignment_literal))))

================
SET variable reference in unquoted value
================

SET path=%userprofile%\scripts

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (assignment_value
      (variable_reference)
      (assignment_literal))))

================
SET variable reference in quoted SET form
================

SET "path=%userprofile%\scripts"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (quoted_assignment_value
      (variable_reference)
      (assignment_literal))))

================
SET variable with parens in variable name
================

SET path=%programfiles(x86)%\app

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (assignment_value
      (variable_reference)
      (assignment_literal))))

================
SET multiple variable references in assignment
================

SET cmd=%windir%\system32\%tool%.exe

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (assignment_value
      (variable_reference)
      (assignment_literal)
      (variable_reference)
      (assignment_literal))))

================
SET variable with quoted paths containing variable references
================

SET gimp_loc="%userprofile%\AppData\Local\Programs\GIMP" "%programfiles(x86)%\GIMP" "%programfiles%\GIMP"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (assignment_value
      (assignment_literal)
      (variable_reference)
      (assignment_literal)
      (variable_reference)
      (assignment_literal)
      (variable_reference)
      (assignment_literal))))

================
SET value with trailing literal percent
================

set var=100%

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (assignment_value
      (assignment_literal)
      (assignment_literal))))

================
SET value with literal exclamation
================

set msg=Hello World!

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (assignment_value
      (assignment_literal)
      (assignment_literal))))

================
SET pseudo-array variable with brackets
================

set "arr[1]=hello"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (quoted_assignment_value
      (assignment_literal))))

================
SET pseudo-array variable unquoted
================

set arr[0]=world

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (assignment_value
      (assignment_literal))))

================
Positional parameters in arguments
================

echo %1 %2 %~f1

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference)
      (variable_reference)
      (variable_reference))))

================
Parameter modifier %~dp0
================

echo %~dp0

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
Delayed expansion variable
================

echo !MY_VAR!

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
Substring extraction
================

echo %VAR:~0,5%

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
String replacement in variable
================

echo %PATH:old=new%

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
FOR loop variable in arguments
================

echo %%i

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
SET variable with @ prefix
================

set "@amp=x"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (quoted_assignment_value
      (assignment_literal))))

================
SET variable with double space before quote
================

set  "foo=bar"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (quoted_assignment_value
      (assignment_literal))))

================
Variable reference with dot in name
================

echo %PopArg.ARGS%

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
Variable reference substring replacement
================

echo %PopArg.ARGS:/?="/?"%

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
SET with dynamic variable name via parameter
================

set "%~1=value"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_reference)
    (quoted_assignment_value
      (assignment_literal))))

================
SET variable name starting with slash
================

set "/MACRO=tail"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (quoted_assignment_value
      (assignment_literal))))

================
SET variable name with angle brackets
================

set ">>LOGFILE=>>%LOGFILE%"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (quoted_assignment_value
      (assignment_literal)
      (variable_reference))))

================
SET variable name with dot and brackets
================

set ">MSGOUT.V[0]=rem"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (quoted_assignment_value
      (assignment_literal))))

================
Symbolic one-char variable reference
================

echo %&%

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
SET with caret-escaped quotes
================

set ^"XCALL=call foo^"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (caret_quoted_assignment_value
      (assignment_literal))))

================
SET caret-escaped quotes with literal quote inside
================

set ^"@quot=""

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (caret_quoted_assignment_value
      (assignment_literal))))

================
Delayed expansion of positional param with substitution
================

call !%1:%%=%%%%!

---

(program
  (call_stmt
    (variable_reference)))

================
Variable reference with angle-prefix name
================

echo %>DEBUGOUT%

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
Variable reference with double angle prefix
================

echo %>>LOGFILE%

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
Variable reference with slash-prefix name
================

echo %/MACRO%

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
Variable reference for literal newline
================

echo %\n%

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
All-args variable reference
================

echo %*

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
SET variable literally named percent
================

set "%%=%%"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (quoted_assignment_value
      (assignment_literal)
      (assignment_literal))))

================
SET with quoted variable name containing quotes
================

set ""ARG"=No"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (quoted_assignment_value
      (assignment_literal))))

================
Variable reference with quoted name
================

echo %"ARG"%

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
SET unquoted variable name with hash suffix
================

set RETURN#=exit /b

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (assignment_value
      (assignment_literal))))

================
SET quoted name with FOR variable pattern
================

set "%%v="

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)))

================
SET variable name with variable reference prefix
================

set "%RETVAR%.TYPE=value"

---

(program
  (variable_assignment
    (set_keyword)
    (variable_reference)
    (variable_name)
    (quoted_assignment_value
      (assignment_literal))))

================
SET unquoted with dollar-prefix name
================

set $r=B

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (assignment_value
      (assignment_literal))))

================
SET value with multi-line parenthesized continuation
================

set RETURN=foo ( %\n%
  set A=B %\n%
)

---

(program
  (variable_assignment
    (set_keyword)
    (variable_name)
    (assignment_value
      (assignment_literal)
      (assignment_paren_group
        (assignment_literal)
        (variable_reference)
        (assignment_literal)
        (variable_reference)))))
