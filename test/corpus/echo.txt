================
Echo off
================

@echo off

---

(program
  (echo_off))

================
Echo on
================

@ECHO ON

---

(program
  (echo_off))

================
Echo command
================

echo Hello World

---

(program
  (cmd
    (command_name)
    (argument_list
      (argument_value)
      (argument_value))))

================
Echo with trailing close paren
================

echo will be used.)

---

(program
  (cmd
    (command_name)
    (argument_list
      (argument_value)
      (argument_value)
      (argument_value)
      (argument_value))))

================
Echo lone close paren with redirect
================

echo ) >> ..\out.cmd

---

(program
  (redirect_stmt
    (cmd
      (command_name)
      (argument_list
        (argument_value)))
    (redirection
      (redirect_op)
      (redirect_target))))

================
Echo with quadruple percent for-variable
================

echo %%%%i

---

(program
  (cmd
    (command_name)
    (argument_list
      (variable_reference))))

================
Echo with trailing open paren and redirect
================

echo FOR /F %%%%i in (..\x) do ( >> out.cmd

---

(program
  (redirect_stmt
    (cmd
      (command_name)
      (argument_list
        (argument_value)
        (argument_value)
        (variable_reference)
        (argument_value)
        (argument_value)
        (argument_value)
        (argument_value)
        (argument_value)
        (argument_value)))
    (redirection
      (redirect_op)
      (redirect_target))))
